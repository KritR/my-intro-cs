# Chapter 1

```rust,editable
#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!\n");
   return 0;
}
```

```c,editable
fn main() {
   // printf() displays the string inside quotation
   return 0;
}
```
